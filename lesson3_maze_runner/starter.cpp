// Lesson 3: The Maze Runner (no dead ends)
// Read instructions.md before starting!

#include "../common/maze_types.h"
#include "../common/maze_utils.h"
#include <vector>

// TODO: Follow the maze from `start` to the exit.
//
// This maze has NO dead ends and NO branches: at every cell (other than
// the entrance/exit) there are exactly two openings -- the one you just
// came through, and exactly one other. Your job is to always step through
// the "other" opening, until you reach the exit.
//
// Check directions in this order every time: NORTH, EAST, SOUTH, WEST.
//
// Return the sequence of positions visited, starting with `start` and
// ending with the exit cell.
std::vector<Position> runMaze(const Maze& maze, Position start) {
    std::vector<Position> path;
    // your code here
    return path;
}
